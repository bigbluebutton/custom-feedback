version: '3'
services:
  app:
    build: .
    network_mode: host
    ports:
      - "3009:3009"
    environment:
      - FEEDBACK_URL=https://feedback.requestcatcher.com/
      - LOG_LEVEL=info
      - LOG_STDOUT=true
      - PORT=3009
      - REDIS_HOST=localhost
